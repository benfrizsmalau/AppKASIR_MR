[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"
  NEXT_TELEMETRY_DISABLED = "1"

# Pastikan env vars tersedia untuk Next.js server functions (server actions)
[functions]
  node_bundler = "nft"

[[plugins]]
  package = "@netlify/plugin-nextjs"
